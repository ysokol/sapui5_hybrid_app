<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core" xmlns:form="sap.ui.layout.form">
	<Dialog title="Please specify new visit details: {Route} / {Visit}">
		<form:Form width="95%">
			<form:layout>
				<form:ResponsiveGridLayout/>
			</form:layout>
			<form:formContainers>
				<form:FormContainer>
					<form:FormElement label="Customer">
						<form:fields>
							<ComboBox selectedKey="{Customer}" items="{ path: '/Customers', sorter: { path: 'Customer' }, templateShareable : true }">
								<core:Item key="{Customer}" text="{Description}"/>
							</ComboBox>
							<Label text="{Customer}"/>
						</form:fields>
					</form:FormElement>
				</form:FormContainer>
			</form:formContainers>
		</form:Form>
		<beginButton>
			<Button text="Cancel" press="onSalesOrderItemCancel"/>
		</beginButton>
		<endButton>
			<Button text="Confirm" press="onSalesOrderItemConfirm"/>
		</endButton>
	</Dialog>
</core:FragmentDefinition>